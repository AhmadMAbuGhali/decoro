class AppConstants {
  static const String appName = 'Decoro';
  static const int requestTimeoutSeconds = 15;

  // Pagination
  static const int defaultPageLimit = 12;

  // Headers
  static const Map<String, String> defaultJsonHeaders = {
    'Content-Type': 'application/json',
  };
}